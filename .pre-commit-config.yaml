default_language_version:
  python: python3.12
repos:
  - repo: local
    hooks:
      - id: format
        name: format
        entry: just format
        require_serial: true
        language: system
        pass_filenames: false