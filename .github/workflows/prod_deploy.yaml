name: Deploy to Prod

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy (TODO)
        run: |
          echo "TODO: publish artifact (immutable, tagged)"
          echo "TODO: update ECS service (web/worker)"
          echo "TODO: monitor window + rollback plan"
          exit 0
